<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <artifactId>wb-mmb</artifactId>
    <groupId>org.ihtsdo</groupId>
    <version>2.5.1-trek-inf-1-SNAPSHOT</version>
  </parent>

  <artifactId>wb-docs</artifactId>
  <name>Generate documentation from DITA sources</name>

  <description>Take a set of DITA files and transform them to various output formats using the
    DITA Open Toolkit. The output format produced is controlled by maven profiles - see the end
    of the pom.xml file. </description>

  <packaging>pom</packaging>

  <properties>
    <dita.dir>${project.build.directory}/dependency/DITA-OT1.5.1</dita.dir>
    <!-- User must specify this
    <xep.dir>...</xep.dir>
     -->
  </properties>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>unpack-dita-ot</id>
            <goals>
              <goal>unpack</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>net.sf.dita-ot</groupId>
                  <artifactId>dita-ot-minimal</artifactId>
                  <version>1.5.1-M6</version>
                  <type>tar.gz</type>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
          <execution>
            <id>add-transform-libs</id>
            <goals>
              <goal>copy</goal>
            </goals>
            <configuration>
              <outputDirectory>${dita.dir}/lib</outputDirectory>
              <artifactItems>
                <artifactItem>
                  <groupId>net.sourceforge.saxon</groupId>
                  <artifactId>saxon</artifactId>
                  <version>9.1.0.8</version>
                </artifactItem>
                <artifactItem>
                  <groupId>net.sourceforge.saxon</groupId>
                  <artifactId>saxon</artifactId>
                  <version>9.1.0.8</version>
                  <classifier>dom</classifier>
                </artifactItem>
                <artifactItem>
                  <groupId>com.ibm.icu</groupId>
                  <artifactId>icu4j</artifactId>
                  <version>3.4.4</version>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
        </executions>
      </plugin>
    
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <executions>
          <execution>
            <id>enforce-property</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireProperty>
                  <property>xep.dir</property>
                  <message>Please ensure that the xep.dir is specified in your settings.xml</message>
                </requireProperty>
                <requireProperty>
                  <property>env.ANT_HOME</property>
                  <message>Please ensure that the ANT_HOME environment variable is set to point to your ANT installation</message>
                </requireProperty>
              </rules>
              <fail>true</fail>
            </configuration>
          </execution>
        </executions>
      </plugin>
    
      <!--  load ${timestamp} -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>buildnumber-maven-plugin</artifactId>
        <version>1.0-beta-2</version>

        <executions>
          <execution>
            <phase>validate</phase>

            <goals>
              <goal>create</goal>
            </goals>

            <configuration>
              <timestampFormat>{0,date,MMMMM dd yyyy}</timestampFormat>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- put build properties into an xsl file to be consumed by custom.xsl -->

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>

    <!--  TODO: simplify -->
        <executions>
          <execution>
            <id>make-doc-date</id>

            <phase>generate-sources</phase>

            <goals>
              <goal>copy-resources</goal>
            </goals>

            <configuration>
              <resources>
                <resource>
                  <directory>src/main/resources</directory>

                  <filtering>true</filtering>
                </resource>
              </resources>

              <outputDirectory>${project.build.directory}/dita/resources</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>dita-maven-plugin</artifactId>
        <version>1.0-beta-2</version>
        <executions>
          <execution>
            <id>dita2pdf</id>
            <phase>process-resources</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <useDitaAnt>false</useDitaAnt>
              <useDitaClasspath>true</useDitaClasspath>
              <!-- attach the generated PDF: this doesn't work in the current version of the dita-maven-plugin
                   when the ditamap is in a subdirectory, so it is added using the build-helper below 
              <attach>true</attach>
              -->
              <antProperties>
                <transtype>pdf2</transtype>
                <dita.dir>${dita.dir}</dita.dir>
                <args.input>src/main/dita/Guidebook/Workbench_Guidebook.ditamap</args.input>
                <dita.extname>dita</dita.extname>
                <output.dir>${project.build.directory}/pdf</output.dir>
                <xep.dir>${xep.dir}</xep.dir>
                <pdf.formatter>xep</pdf.formatter>

                <!--                
                <customization.dir>${customization.dir}</customization.dir>
                <clean.temp>yes</clean.temp>
        -->
              </antProperties>
<!-- Should not be needed, as only XEP needs the extra memory
              <antOpts>-Xmx1600m -Xms1100</antOpts>
-->
            </configuration>
          </execution>
        </executions>
      </plugin>
      
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <executions>
          <execution>
            <id>add-dita-sources</id>
            <configuration>
              <descriptors>
                <descriptor>src/main/assembly/dita-sources.xml</descriptor>
              </descriptors>
            </configuration>
            <goals>
              <goal>single</goal>
            </goals>
            <phase>package</phase>
          </execution>
        </executions>
      </plugin>
      
      <!-- to workaround dita-maven-plugin's attach configuration problems -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>attach-pdf</id>
            <goals>
              <goal>attach-artifact</goal>
            </goals>
            <configuration>
              <artifacts>
                <artifact>
                  <file>${project.build.directory}/pdf/Guidebook/Workbench_Guidebook.pdf</file>
                  <type>pdf</type>
                </artifact>
              </artifacts>
            </configuration>
          </execution>
        </executions>
      </plugin>
      
      <!-- Coerce the DITA scripts into using more memory for XEP -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <id>adjust-memory</id>
            <phase>generate-resources</phase>
            <configuration>
              <tasks>
                <replace file="${dita.dir}/demo/fo/build.xml" token='maxmemory="256m"' value='maxmemory="1024m"' />
              </tasks>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
